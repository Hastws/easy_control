@PACKAGE_INIT@

# 导入 autoalg_all 的目标（system_output、mac_bridge*）
include("${CMAKE_CURRENT_LIST_DIR}/autoalg_allTargets.cmake")

# 同时把你单独导出的 input_sim 也包含进来，便于统一使用
if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/../autoalg_input_sim/input_simTargets.cmake")
  include("${CMAKE_CURRENT_LIST_DIR}/../autoalg_input_sim/input_simTargets.cmake")
elseif(EXISTS "${CMAKE_CURRENT_LIST_DIR}/input_simTargets.cmake")
  include("${CMAKE_CURRENT_LIST_DIR}/input_simTargets.cmake")
endif()